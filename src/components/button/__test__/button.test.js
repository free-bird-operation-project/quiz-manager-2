'use strict'

import { Button } from '../button.js'

describe('Button', () => {
	describe('constructor', () => {
		it('should set default properties when a new instance is created', () => {
			// My test code for constructor here
		})
	})

	describe('isValidConfig()', () => {
		it('should be called when createNode() is triggered', () => {
			// My test code for isValidConfig being called
		})

		it('should call verifyConfig() once', () => {
			// My test code for verifyConfig being called once
		})
	})

	describe('createNode()', () => {
		it('should call createNodeButton() if this._isValidConfig() is true', () => {
			// My test code for createNode calling createNodeButton
		})

		it('should return true if this._isValidConfig() is true', () => {
			// My test code for createNode returning true
		})

		it('should return false if this._isValidConfig() is false', () => {
			// My test code for createNode returning false
		})
	})

	describe('removeNode()', () => {
		it('should call removeNodeButton() when called', () => {
			// My test code for removeNode calling removeNodeButton
		})
	})
})
